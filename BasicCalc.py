# by default input() takes data in string type
# we have to type cast/conversion
fnum = int(input("Enter first number : "))
snum = int(input("Enter second number : "))

add = fnum + snum
print("Sum is",add)

sub = fnum - snum
print("Diff is",sub)

div = fnum / snum
print("Div is",div)

mul = fnum * snum
print("Mul is",mul)
